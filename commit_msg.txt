refactor(build_stream): domain-segregate deploy/restart pipelines with critical fixes

Move pxe_buildstream_manager role from utils/ to build_stream/roles/.
Create deploy_build_stream.yml and restart_build_stream.yml playbooks
that invoke the orchestrator as a subprocess and handle BSM
post-processing independently.

Critical fixes found during end-to-end trace:
- Fix ARTIFACTS_DIR path in playbook watcher: NFS_SHARE_PATH/omnia/
  build_stream_root added spurious /omnia/ segment. Now uses
  OMNIA_DATA_PATH/build_stream_root directly.
- Fix restart post-processing: orchestrator runs as subprocess so
  Ansible facts (groups['bmc'], hostvars) are unavailable. Now reads
  orchestrator_status.yml and failed_nodes.json from orchestrator
  output directory instead.
- Add credential loading (provision_password) for GitLab SSH in
  restart playbook -- orchestrator credentials are not shared across
  subprocess boundary.
- Copy failed_nodes.json to artifacts dir alongside node_results.json
  so result_poller can find both files for artifact persistence.

API changes:
- deploy_use_case.py: submit deploy_build_stream.yml (no tags)
- create_restart.py: submit restart_build_stream.yml (no tags)
- playbook_paths.conf: add new playbook entries, remove set_pxe_boot

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
